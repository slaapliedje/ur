# ProDOS-8 SYSTEM program that uses double-hi-res PAGE 2 ($4000-$5FFF).
#
# Loads at $2000 like a normal SYSTEM file (clean ProDOS boot). STARTUP sits at
# $2000; CODE is pinned at $6000, so the $2034-$5FFF gap (which contains the DHGR
# page-2 bitmap at $4000-$5FFF) is left as zero-fill and free at run time. One
# MAIN region keeps __MAIN_START__/__MAIN_LAST__ defined for the EXEHDR.
#
# BSS lives at $2100-$3FFF — main-bank RAM under the page-2 bitmap that the
# renderer never touches (it displays PAGE 2 only; $2000-$3FFF is hi-res PAGE 1,
# unused). The old layout squeezed BSS between ONCE and the C stack at the top
# of MAIN, where it no longer fits: code growth pushed ONCE past $9000, making
# that area NEGATIVE-sized — ld65 2.18 silently let BSS spill into the stack
# region, current ld65 (CI builds cc65 master) rightly refuses to link it.

SYMBOLS {
    __EXEHDR__:    type = import;
    __FILETYPE__:  type = weak, value = $00FF; # ProDOS SYS
    __STACKSIZE__: type = weak, value = $0600; # 1.5k C stack below $9600 (shallow,
                                               # turn-based call depth)
    __LCADDR__:    type = weak, value = $D400;
    __LCSIZE__:    type = weak, value = $0C00;
}
MEMORY {
    ZP:     file = "", define = yes, start = $0080,         size = $001A;
    HEADER: file = %O,               start = $2000 - $003A, size = $003A;
    MAIN:   file = %O, define = yes, start = $2000,         size = $9600 - $2000;
    # Overlaps MAIN's zero-fill gap on purpose: file="" emits nothing, ProDOS
    # zero-fills the gap at load, and crt0 zeroes BSS again at startup.
    BSS:    file = "",               start = $2100,         size = $4000 - $2100;
    LC:     file = "", define = yes, start = __LCADDR__,    size = __LCSIZE__;
}
SEGMENTS {
    ZEROPAGE: load = ZP,             type = zp;
    EXEHDR:   load = HEADER,         type = ro,  optional = yes;
    STARTUP:  load = MAIN,           type = ro;
    LOWCODE:  load = MAIN,           type = ro,  optional = yes;
    CODE:     load = MAIN,           type = ro,  start = $6000;
    RODATA:   load = MAIN,           type = ro;
    DATA:     load = MAIN,           type = rw;
    INIT:     load = MAIN,           type = rw;
    ONCE:     load = MAIN,           type = ro,  define   = yes;
    LC:       load = MAIN, run = LC, type = ro,  optional = yes;
    BSS:      load = BSS,            type = bss, define   = yes;
}
FEATURES {
    CONDES: type    = constructor,
            label   = __CONSTRUCTOR_TABLE__,
            count   = __CONSTRUCTOR_COUNT__,
            segment = ONCE;
    CONDES: type    = destructor,
            label   = __DESTRUCTOR_TABLE__,
            count   = __DESTRUCTOR_COUNT__,
            segment = RODATA;
    CONDES: type    = interruptor,
            label   = __INTERRUPTOR_TABLE__,
            count   = __INTERRUPTOR_COUNT__,
            segment = RODATA,
            import  = __CALLIRQ__;
}
